#include <bits/stdc++.h>

using namespace std;

using i32 = int;

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);

    int n, k;
    cin >> n >> k;

    vector<i32> a(n);

    for (int i = 0; i < n; i++) {
        cin >> a[i];
    }
}
